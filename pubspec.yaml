name: newsapp
description: A news application

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.14.4 <3.0.0"

dependencies:
  http: ^0.13.4
  provider: ^6.0.1
  shimmer: ^2.0.0
  carousel_slider: ^4.0.0
  share_plus: ^3.0.4

  flutter:
    sdk: flutter


dev_dependencies:
  flutter_lints: ^1.0.0

  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true

  assets:
    - assets/img/logo.png